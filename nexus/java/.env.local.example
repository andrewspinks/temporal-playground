# Local dev server. Copy to .env:  cp .env.local.example .env
#
# Selects the local profiles from temporal.toml instead of the Cloud ones. Address and namespace
# come from those profiles — deliberately not set here, because a TEMPORAL_* var would override
# every profile at once.

HANDLER_PROFILE=handler-local
CALLER_PROFILE=caller-local

[project]
name = "nexus-hello"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "temporalio==1.26.0",
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

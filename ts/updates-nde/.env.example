# Copy to .env and fill in your values.
# .env is git-ignored — never commit secrets.

# Temporal Cloud API key (used with [profile.cloud] in temporal.toml)
TEMPORAL_API_KEY=your-api-key-here

# Optionally pin the active profile (overrides the default profile)
# TEMPORAL_PROFILE=cloud
